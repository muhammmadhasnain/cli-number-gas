#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir//urs/bin/env" ]; then
  exec "$basedir//urs/bin/env" node "$basedir/../@muhammadhasnian/cli-number-gas/index.js" "$@"
else 
  exec /urs/bin/env node "$basedir/../@muhammadhasnian/cli-number-gas/index.js" "$@"
fi
